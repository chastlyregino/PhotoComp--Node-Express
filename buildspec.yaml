version: 0.2
phases:
  install:
      runtime-versions:
        nodejs: 18
      commands:
        - npm install
        - npm install -g typescript
  build:
      commands:
        - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: dist