version: 0.2
phase:
  install:
      runtime-versions:
        nodejs: 18
      commands:
        - npm install -g typescript
        - npm install --save-dev typescript
        - npm install
        - tsc --init
artifacts:
  files:
    -"**/*"